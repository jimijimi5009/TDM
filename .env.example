# Example .env file for backend credentials
# In a real application, these would be securely managed and not committed to version control.

DB_USER=your_db_username
DB_PASSWORD=your_db_password
PORT=3000
